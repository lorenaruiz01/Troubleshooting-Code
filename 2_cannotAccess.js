// Access errors

let firstName = "Alex";

console.log( firstName );
console.log( lastName );

let lastName = "Miller";

// lastName cannot be accessed because it has not been defined at the time that the console tries to log it  
// move the line up to line 4 in order to declare it be able to console log it. 